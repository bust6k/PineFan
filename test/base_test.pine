switch f
    => indicator("no")

foo = "RMA"

switch foo
   fooa => indicator("soo")
   "RMA" => strategy("ll")
   "WMA" => indicator("it's a fooo")
   => indicator("cant process")

switch foo
   foo => indicator("sorry")
   "FOO" => strategy("real")
   => indicator("gonna out!")
